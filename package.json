{
  "name": "tension-de-surface",
  "private": true,
  "description": "Prototype « Tension de surface » — le jeu s'ouvre sans build (index.html) ; Playwright ne sert qu'au test de fumée.",
  "scripts": {
    "test": "node test/smoke.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
